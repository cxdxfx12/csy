# 2026-06-09

## AI助手修复: pendingRepair 非响应式 Bug (0:28)
- 用户反馈：业主端AI助手点不了、没有确认按钮
- 根因：`AiChatWidget.vue` 中 `pendingRepair` 使用 `let` 声明而非 `ref()`，Vue 无法追踪变化
- 修复：
  - `let pendingRepair = null` → `const pendingRepair = ref(null)`
  - 所有脚本中引用加 `.value`（共7处）
  - 修复 `saveLastOrder()` 在 `pendingRepair.value = null` 后取值导致标题丢失的 bug（先存 `savedTitle`）
- 构建部署：owner.html + owner-BBu9P7L-.css + owner-DQXcxKVq.js 上传到服务器 `/www/wwwroot/www.hbdxm.com/public/owner/`
- commit: f160943 fix: AI助手 pendingRepair 非响应式导致确认按钮不显示
- GitHub 推送仍未成功：SSH 公钥未添加到 GitHub 账号（公钥：ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIGK5CeqmyMr2XPnuiwGA7PQyZ2JQW2X4SYPnFcMN8XPf cxdxfx12@github）

## AI助手第二次修复: 根因是构建 base URL + 关键词误匹配 (0:45)
- 用户反馈：AI助手还是不行
- **根因1（JS 404）**：构建缺少 `--base /owner/`，导致 HTML 引用 `/assets/xxx.js`，但文件实际在 `/owner/assets/xxx.js` → 整个 Vue APP 未加载
  - 修复：构建命令改为 `npm run build -- --outDir ../public/owner --base /owner/`
  - HTML 引用已修正为 `/owner/assets/owner-DQXcxKVq.js`
- **根因2（关键词误匹配）**：`'进度'`、`'查一下'`、`'帮我查'`、`'怎么样了'`、`'什么时候修'` 太宽泛，用户描述报修内容时容易误触发进度查询
  - 例如："厨房漏水，帮我查一下" → 匹配"帮我查" → 走进度查询而非报修流程
  - 修复：删除 5 个宽泛关键词，只保留 7 个明确的：查进度、工单状态、处理进度、修好了吗、报修进度、我的报修、工单查询
- **根因3（pendingRepair 非响应式）**：上轮已修复（let → ref）
- 部署：HTML 3文件 + AiRepair.php 上传服务器
- commit: 56b051f fix: 构建base URL不匹配导致JS 404 + 关键词误匹配阻塞报修
- ⚠️ 重要：以后构建 owner 前端必须加 `--base /owner/`

## 回退到 8c451bf + PHP 语法修复 (1:10)
- 用户反馈：AI助手和报修数据没打通，看不到工单处理进度。要求恢复到 00:16 状态
- 执行：`git reset --hard 8c451bf`（AI报修助手与工单系统打通 - 进度查询+业主关联）
- 重新构建 + 全部文件部署到服务器
- **PHP 语法错误修复**：`AiRepair.php` 第 207 行中文引号 `"查进度"` 用了 ASCII U+0022，PHP 解析为字符串终止符
  - 修复：改为 `「查进度」`（中文角括号）
  - 测试通过：API 返回 code=0，正确识别报修类型
- 服务器验证通过：/api/ai/chat 返回 200，AI 正常响应
- 被丢弃的 2 个 commit：f160943、56b051f（修复尝试反而坏了更多）

## Git Tag: stable-2026-06-09-0107 (1:07)
- 提交 fe0b9f6：PHP语法修复 + 构建产物更新
- Tag: `stable-2026-06-09-0107` — 标注为稳定版本，AI报修助手+工单打通，业主端完整可用
- 恢复命令：`git checkout stable-2026-06-09-0107`

## AI报修自动提取业主房产+电话 (1:31)
- 用户要求：提交工单时自动带出业主绑定的房产信息和电话号码
- **改了什么：**
  - `AiRepair.php` `submit()`：找到业主后，自动查询 `ds_room WHERE owner_id=$ownerId` 获取 room_id，写入工单
  - `AiChatWidget.vue` `doSubmit()`：去掉 `name: '业主'` 和 `phone: ''` 硬编码，全部由后端从账户提取
- **原来缺什么：** room_id 只靠 location 文本模糊匹配房间号，基本失败；现在直接从 owner 绑定的 room 表取
- **电话已具备：** 后端 `$phone = $phone ?: $owner['phone']` 已有 fallback，加 room 查询后更完整
- commit: 75de889 feat: AI报修自动提取业主房产信息(room_id)和电话号码
- 服务器部署验证：PHP 语法通过，API chat 正常返回 confirm

## AI报修 JWT Token 认证修复 (1:38)
- **问题现象**：后台工单管理中"业主"报修的工单全部 owner_id=0, room_id=0, phone=未提供
- **根因**：`getOwnerIdFromToken()` 错误地把 JWT 字符串当数据库字段去查 `ds_owner.token` 列 — 但该列根本不存在！
  - 登录系统用的是 JWT（`Firebase\JWT\JWT::encode/decode`），token 的 payload 里 `sub` = owner_id
  - `BaseApi::auth()` 正确做法是 JWT::decode 取 sub，但 AiRepair 自己写了个错误的查表逻辑
- **修复**：
  - `AiRepair.php` 引入 `use Firebase\JWT\JWT; use Firebase\JWT\Key;`
  - `getOwnerIdFromToken()` 改为与 `BaseApi::auth()` 一致的 JWT 解码逻辑
- commit: 8bc5935 fix: AI报修getOwnerIdFromToken改用JWT解码
- ⚠️ 已部署到服务器，下次业主提交AI报修即可生效。旧工单不会自动修正（owner_id=0）

## 独立版AI聊天无token根因定位与修复 (01:57-02:03)
- **关键发现**：用户使用的是 `public/3d-yanshi/ai-chat.js`（数字孪生页面的独立AI聊天），跟 `frontend/src/owner/components/AiChatWidget.vue`（业主SPA的Vue版）是**两套完全不同的代码**！
- **根因**：`ai-chat.js` 的 `submitRepair()` 完全没有 token 逻辑 — 没有从 localStorage 读 owner_token，没有 Authorization 头，硬编码 `phone: ''` 和 `name: 'AI报修用户'`
- **调试过程中的误判**：加了 `runtime_path()` 调试代码导致 PHP 报错（函数不存在），用户显示"系统暂时无法处理"
- **修复**：
  1. `ai-chat.js`：添加 `getOwnerToken()` 从 localStorage 读 owner_token，`makeHeaders()` 构建带 Authorization 的请求头
  2. `chatWithAI()` 和 `submitRepair()` 都改用 `makeHeaders()`
  3. `AiRepair.php`：清理调试代码，保留干净的 JWT 解码
- **关键前提**：用户需先在业主端 SPA 登录（手机号或微信），localStorage 才会有 `owner_token`。3d-yanshi 页面本身没有登录功能
- commit: 886d6f5
- 服务器 .env JWT 密钥：`d5F8kL2mN9pQ3rT7wX1yA4bC6eH0jV5u`（与 config/jwt.php 默认值不同，但 config() 会从 .env 读取）

## 后台工单管理加电话列 (1:42)
- 用户要求：工单管理列表加上电话列
- 后端数据已包含 `reporter_phone`（`ro.*`），只改前端
- `RepairOrder.vue`：在报修人列后加 `<el-table-column prop="reporter_phone" label="电话" width="130" />`
- 构建部署：admin 全部 249 个文件上传到服务器
- commit: feat: 后台工单管理列表增加电话列

## AI报修类型永远显示"水" + 手机号反查业主 (02:05-02:12)
- **型总是"水"根因**：`repair_order` 表 `type tinyint(1)` 默认 1（注释：1水 2电 3气 4门窗 5管道 6家电 7网络 8其他），但 `submit()` 从来没把 AI 识别的 `repair_type` 写入 `$orderData['type']`，全部取默认值 1 = 水
- **手机号反查业主**：3d-yanshi 页无登录，但用户不想登录两次。改为提交前先问一次手机号，后端通过手机号反查 owner 表和 room 表，自动填房号和姓名
- **修复**：
  1. `AiRepair.php` 新增 `mapRepairType()`：将 AI 字符串类型（水电/空调/门窗等）映射为数据库数字 ID，水电细分：电相关关键词→2(电)，否则→1(水)
  2. `AiRepair.php` 新增 `findOwnerByPhone()`：手机号→查 owner→查 room，返回业主+房产数据
  3. `AiRepair.php->submit()`：无 token 但有手机号时调用反查；将 `type` 和 `room_number` 写入结果回复
  4. `ai-chat.js`：无 token 且无手机号时，「确认」→先问手机号→拿到后提交→后端反查
- commit: 74f354a fix: AI报修类型映射+手机号反查业主房产

## AI报修三大改进 (02:14-02:18)
### 1. 确认提交弹窗（含手机号输入框）
- **问题**：之前在聊天框里问手机号，用户容易忽略或跳过
- **修复**：`ai-chat.js` 改为点击「确认」后弹出模态对话框 `#ai-confirm-overlay`，展示类型/位置/描述摘要 + 手机号输入框。已登录可不填。同时加了完整CSS样式到 `ai-chat.css`
### 2. 类型映射优化
- **问题**：裂缝→墙面→type=8(其他)，所有非水电/空调/门窗的类型都映射到8
- **修复**：`mapRepairType()` 调整映射表：墙面→5(管道)，电梯→4(门窗)
### 3. 按工人类型智能派单
- **问题**：之前 `order('RAND()')` 随机派单，不管什么类型的工单
- **修复**：
  - 新增 `aiTypeToWorkerTypes()` 将AI类型映射到工人表的 type 关键词（如"水电"、"家电"、"门窗"）
  - 新增 `assignWorkerByType()` 先按 `repair_worker.type LIKE '%关键词%'` 匹配，按 order_count 升序选最闲的；匹配不到再兜底随机选
  - 派单逻辑从 submit() 中替换调用
- commit: 74e8cfc feat: AI报修确认弹窗+类型映射优化+按类型智能派单

## fix: orderRaw不存在 (02:23)
- **错误**：`Call to undefined method think\facade\Query::orderRaw()`，ThinkPHP Query 没有 `orderRaw()` 方法
- **修复**：`assignWorkerByType()` 中两处 `->orderRaw('order_count ASC, RAND()')` 改为 `->order(Db::raw('order_count ASC, RAND()'))`
- commit: 4c16e0a fix: orderRaw不存在,改用Db::raw排序

## fix: 用PHP array_rand代替orderRaw (02:27-02:30)
- **问题**：用户仍然报 `orderRaw()` 错误。原因1：上次 `Db::raw()` 可能没正确部署；原因2：即使部署了，旧文件被 PHP opcache 缓存
- **修复**：`assignWorkerByType()` 彻底去掉所有 raw 方法，改为：
  ```php
  $workers = Db::name('repair_worker')...->order('order_count asc')->select()->toArray();
  return $workers[array_rand($workers)];
  ```
  在 PHP 层面随机选，完全兼容所有 ThinkPHP 版本
- SSH 需要用 kex algorithms override 才能连接（`algorithms: {kex: [...]}`）
- 部署后执行 `systemctl reload php-fpm` 清除 opcache
- commit: c9a7c66 fix: orderRaw不存在,改用PHP层面array_rand随机 + 清除opcache

## 业主端AiChatWidget修复 (02:35-02:45)
### 问题1: 业主端用的是 AiChatWidget.vue，不是 ai-chat.js
- **发现**：用户通过 `www.hbdxm.com/owner.html`（Vue SPA）使用AI报修，用的是 `frontend/src/owner/components/AiChatWidget.vue`
- 之前一直在改 `public/3d-yanshi/ai-chat.js`，但用户根本没用到那个！
### 问题2: select()返回数组,不能调toArray()
- ThinkPHP 8 的 `select()` 直接返回 array，`toArray()` 报错 `Call to a member function toArray() on array`
### 问题3: owner_id=0, phone=未提供
- 服务器数据：工单 ID 37(DSR202606090238039716) owner_id=0, reporter_phone=未提供
- 业主老程(ID=254)有手机号 19171045360，说明 token 没传到或JWT解码失败
- 可能原因：微信一键登录的token格式与JWT不一致，或localStorage的key不匹配
### 修复:
1. **AiChatWidget.vue** 确认提交栏增加手机号输入框（📱手机号 input），已登录可留空
2. 提交时把 phone 字段一起发给后端，后端通过手机号反查业主和房产
3. 后端 `assignWorkerByType()` 去掉 `toArray()`，直接用 `select()` 返回值
4. 构建前端(`npm run build`) → 部署6个文件(owner.html+JS/CSS+GlobalToast/pinia)
5. SSH连接需指定 kex algorithms 才能连上服务器
- commit: 10b1dca fix: select()返回数组,去掉toArray()
- commit: a909570 feat: 业主端AI确认栏加手机号输入+后端token调试
- 02:55 用户确认修复生效，报修功能正常工作
- commit: 6e4d743 feat: 报修自动拼接楼栋+单元+房号完整地址
  - room表字段：building_name(楼栋)、unit(单元)、room_number(房号)
  - 有roomData时自动拼完整地址到location字段，格式如 "2栋1单元 4204"
  - AI回复同时显示📍位置和🏠房号

## 经理端风格改为亮色蓝调 (03:53)
- 3个文件改为与业主端一致的亮色蓝调：`App.vue`(body背景), `LoginView.vue`(登录页), `DashboardView.vue`(主面板全部样式)
- 主色调 `#10b981`(翠绿) → `#2563eb`(蓝色)，深色背景 → 白色+阴影
- commit: a283654

## fix: status=2 显示已派单 (03:56-04:03)
- 状态映射表 status=2 从 `'待接单'` → `'已派单'`，status=1 从 `'待派单'` → `'待处理'`
- 同步更新 `progressSteps`
- 部署方式改用 `spawn('scp')` 替代 ssh2 库（ssh2卡住），`spawn`直接调本机scp成功
- commit: 36fc349

## fix: 查进度chat回复显示师傅手机号 (04:16)
- 查进度chat回复中师傅信息从只显示名字改为「赵师傅（手机号）」
- `AiRepair.php`：两次查询都加了 `->field('name,phone')`，chat回复拼接手机号
- 部署时发现之前只传了前端，AiRepair.php没部署

## style: 经理端深色奢华玻璃态重设计 (04:09)
- 3个Vue文件全部重写样式：App.vue(body深空渐变背景), LoginView.vue(毛玻璃卡片), DashboardView.vue(全部组件)
- 设计语言：紫金渐变(#6366f1→#8b5cf6) + 毛玻璃(backdrop-filter:blur) + 彩色统计卡 + 渐变色数字(background-clip:text)
- 构建产物：manager-BrDebxKd.js(35KB) + manager-DL3755jK.css(20KB) + GlobalToast共享文件
- 部署时发现服务器上文件名与本地不匹配（服务器有旧版hash文件），需全量重新部署
- commit: (前端重设计提交)

## GitHub推送踩坑 (04:21)
- 远程URL从 git@github.com 改为 ssh://git@ssh.github.com:443/cxdxfx12/csy.git 才能推送
- GIT_SSH_COMMAND 需要指定 `-i C:/Users/Administrator/.ssh/id_ed25519`（反斜杠路径在cmd中需用正斜杠）
- 推送成功：749c606 fix(2026-06-09 04:21) 提交到master
- 推送完后远程URL已恢复为 git@github.com:cxdxfx12/csy.git

## 电梯模块种子数据 (04:50)
- 三个菜单插入测试数据：电梯台账(12条)、电梯故障(2条未处理)、电梯巡检(3条不合格/整改)
- 覆盖阳光花园(1/2/3/5号楼)、翠苑新城(4栋/5栋/A栋)、怡丰城(1幢)、新龙城
- 表：`ds_elevator`(电梯台账)、`ds_elevator_fault`(故障记录)、`ds_elevator_inspection`(巡检记录)
- SQL文件：`elevator_seed.sql`

## 电梯模块前端显示0排查 (13:12)
- **结论：后端完全正常，数据存在且API返回正确**
- 数据库确认：elevator 12条, fault 16条, inspection 22条，delete_time全NULL
- admin(role_id=1, super_admin)：`getFilteredCommunityId()`返回null，不添加community过滤
- 用PHP生成真实JWT token测试API：
  - `/api/admin/equipment/elevatorList` → code=0, rows=12 ✅
  - `/api/admin/equipment/elevatorFaultList` → code=0, rows=15 (分页) ✅
  - `/api/admin/equipment/elevatorInspectionList` → code=0, rows=15 (分页) ✅
- JWT配置：key=`ds_property_manager_jwt_key_2026`, alg=HS256, type=admin, sub=user_id
- 角色表：1=super_admin, 2=admin, 3=manager, 4=service, 5=finance, 6=security, 7=engineer, 8=community_admin
- `getFilteredCommunityId()`：role_id<=2返回null(不过滤)；role_id>2且有boundCommunityIds返回-1(用in条件)
- 前端显示0原因：可能是浏览器token过期/未登录/缓存，需F12 Network确认请求响应

## fix: 电梯三菜单显示0根因 - API返回格式不匹配 + 漏传主入口JS (13:18-13:35)
- **根因1（代码）**：BaseController::table() 返回 `{code:0, data:[数组], count:N}` 但 Elevator/ElevatorFault/ElevatorInspection.vue 只读 `res.data.list` 和 `res.data.total`（undefined）
  - 修复：三文件改为兼容写法 `(res.data)?.list || res.data || []`
- **根因2（部署）**：只上传了3个电梯chunk JS+CSS，但**漏传了主入口 index-opCKJLY_.js**！Vite动态import的chunk映射在主入口JS里，浏览器一直加载旧版代码引用旧hash
- **教训**：以后修改任何Vue组件后构建，必须同时上传：index.html + 主入口JS(CSS) + 对应的chunk文件
- 最终9文件全量部署成功，等用户确认
- commit 待执行

## fix: 浏览器仍加载旧hash - 全量清理重建部署 (13:49-14:20)
- **现象**：浏览器（包括无痕模式）仍请求不存在的 `index-DpDNpIiD.js`
- **排查**：DNS直连服务器(无CDN)，web_fetch和curl都返回正确新版 `index-opCKJLY_.js`
- **根因**：之前上传时assets目录是root:root属主，nginx(www)无法访问。SFTP虽报成功但文件未实际写入
- **修复**：`chown -R www:www` → 清理全部assets → 重新SFTP上传247个文件 + index.html → 验证246文件 + Elevator-DpDNpIiD.js存在 ✅
-   **⚠️ 重要教训**：以后部署必须确认目标目录属主是 www:www，否则SFTP静默失败
- commit 待执行
