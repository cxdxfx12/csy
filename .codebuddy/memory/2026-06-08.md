# 2026-06-08 工作日志

## 登录页面问题排查与修复
- 用户报告 hbdxm.com/admin/login 登录有问题
- 经测试后端 API 正常（验证码、登录接口均返回正确结果）
- 根因：nginx 缓存了旧版 index.html，用户浏览器引用了已不存在的旧 JS 文件（如 index-DhNUDoAr.js, LoginView-D_m0gu6e.js）
- 修复：在 nginx 配置中添加 `location = /admin/index.html` 规则，设置 no-cache 头
- 全局 JS/CSS 缓存规则修改为排除 /admin/assets/ 路径
- 验证通过：index.html 返回 Cache-Control: no-cache, no-store, must-revalidate

## 登录成功但页面不跳转问题
- 用户报告"显示登录成功，但是页面没有跳转"
- 根因：服务器上缺少前端构建文件 `index-DhNUDoAr.js` 和 `RoleIcon.vue_vue_type_script_setup_true_lang-DdRNPsNZ.js`
- `index-DhNUDoAr.js` 是 Vite 构建的共享 chunk（Vue 运行时等），被 `DashboardView` 动态引用
- 缺少此文件导致 Dashboard 组件加载失败（404），页面停留在 login 不跳转
- 修复：重新 `npm run build` 并将所有 240 个 assets 文件完整同步到服务器
- 验证通过：登录后成功跳转到 /admin/dashboard，页面标题"控制台"正常显示

## hbdxm.com/qh 外部图片本地化
- 启航运维科技网站（qh目录）原先引用了 7 张 picsum.photos 外部图片和 Font Awesome 4.7.0 CDN
- 用 AI 生成替换了 7 张主题相关的图片（IT运维、办公、云计算等），保存到 qh/images/
- 下载 Font Awesome 4.7.0 完整包（CSS + 6个字体文件）到 qh/font-awesome/
- 更新了 5 个 HTML 文件（index/service/solution/advantage/case）的引用路径
- 将 qh 目录复制到 public/qh 并通过 SFTP 部署到服务器
- 验证通过：所有本地资源返回 200，无外部 picsum/cdn 引用

## qh 图片优化 (14:09)
- 用户要求压缩 qh/images/ 图片、去AI水印、转JPG
- 使用 OpenCV (cv2) 检测水印：MSER文字检测 + 亮度异常 + 自适应阈值
- 水印去除：cv2.inpaint (INPAINT_TELEA)，底部20%区域 + 右下角logo检测
- 尺寸：从 1024-1408px → 700px 宽度
- 格式：PNG → JPG (quality=82)
- 效果：9.5MB → ~257KB（97.3% 压缩），7 张图片全部处理
- HTML 引用已全部从 .png 改为 .jpg（qh/ 和 public/qh/ 共 4 个文件）
- 清理了 .bak 备份文件和 _process_images.py 脚本

## qh 图片二次优化 & 部署 (14:09-14:19)
- 用户反馈线上仍显示 .png 引用且 hero-it 右下角 AI 水印未完全去除
- 重新精准水印检测：聚焦半透明白色文字（亮度>150 + 饱和度<60 + Canny边缘），底部25%+右下角区域
- hero-it 右下角重新inpaint (mask 0.14%)
- 问题根因：部署脚本用了 Windows path.join → 远程路径含反斜杠，文件未正确上传
- 修复：所有远程路径改用正斜杠 `REMOTE_DIR + '/' + f.replace(/\\/g, '/')`
- 重新部署 9 个文件（2 HTML + 7 JPG），删除 12 个旧 PNG
- 线上验证通过：index.html/solution.html 均引用 .jpg，hero-it.jpg 返回 200 image/jpeg

## qh 恢复水印 (14:26)
- 用户要求恢复原始水印图片
- 从 git (commit a0f2c32) 恢复 7 张原始 PNG（带AI水印，1024-1408px，共 9.5MB）
- HTML 引用全部从 .jpg 改回 .png（qh/ 和 public/qh/ 共 4 文件）
- 清理所有 .jpg 和 hero-it_orig.png 临时文件
- SSH 部署：删除服务器端 .jpg，上传 .png + HTML
- 线上验证：index.html(200) 和 solution.html(200) 均引用 .png，hero-it.png 返回 200 image/png

## qh PNG转JPG+压缩（保留水印）(14:30)
- 用户要求只做格式转换和尺寸压缩，不去水印
- qh/images/ PNG被PowerShell管道损坏，从git restore恢复后重新转换
- 处理：700px宽度 + JPG quality=82，保留原始水印
- 效果：9.5MB → ~317KB（96.7% 压缩）
- HTML引用 .png → .jpg（4个文件14处引用）
- SSH部署：删远程PNG，上传JPG+HTML，全部验证通过

## WG 物业官网建设 (14:42-14:48)
- 参照 NIO 官网设计风格（深蓝色调 + 渐变 + 大Hero），创建物业官网于 e:\ds\wg\
- 技术选型：纯静态 HTML+CSS+JS（适配现有 PHP/nginx 架构），现代化响应式设计
- 目录结构：wg/css/style.css（23KB）、wg/js/main.js（6KB）、wg/images/
- 4个页面：index（首页Hero+服务+智慧+优势+客户）、solution（住宅/商业/园区/增值）、about（公司+文化+历程）、contact（信息+表单）
- 设计要点：深蓝#1a3a5c主色 + 绿色#2e8b57强调色、数字滚动动画、滚动渐现、移动端响应式、智能数据看板mockup
- 本地复制到 public/wg/，SSH 部署到 /www/wwwroot/www.hbdxm.com/public/wg/
- 线上验证：6个文件全部 200 OK（/wg/ → text/html 17KB，style.css 24KB，main.js 6KB）
- 访问地址：https://www.hbdxm.com/wg/

## WG 官网改为真实公司信息 (14:50)
- 抓取 hbdxm.com 首页获取真实信息：大圣智慧物业系统、杭州喵喵至家网络有限公司
- 品牌色从深蓝+绿 → 深蓝+橙色(#f7931e)，logo改为暖橙渐变色
- 页面重构：index（首页）+ system（系统功能-业主端/员工端/管理后台）+ scene（应用场景-住宅/商业/园区）+ contact（咨询合作）
- 内容全部替换：500+社区/50万+业主/99.9%稳定性/7×24技术支持
- 核心功能：智能报修、投诉建议、车辆管理、公告推送、智能巡检、收费管理
- 三端入口：微信端业主端、移动端员工端、PC管理后台
- 联系方式：17771300068 / mmzj@miaomiaozj.com / 杭州临平区怡丰城12幢
- 删除旧 solution.html/about.html，CSS全面改用橙色系，新增terminals/features/scenes网格样式
- 部署验证：6个文件全部200 OK，style.css 27KB

## WG 咨询留言后端接入 (15:02-15:12)
- 问题：WG联系表单只是模拟提交，数据未保存
- 创建 ds_consultation 表（id/name/phone/type/content/ip/user_agent/status/时间）
- 创建 app/api/controller/Consultation.php — 公开提交接口 POST /api/consultation/add（手机号校验 + IP记录）
- 创建 app/admin/controller/Consultation.php — 管理端 API（lists/detail/delete/batchDelete/markRead/unreadCount）
- 路由注册：route/api.php 添加 public POST + admin CRUD 路由
- 更新 wg/js/main.js：表单改为 fetch POST 到 /api/consultation/add，显示真实成功/失败消息
- 创建 public/admin/consultations.html — 独立管理页面，读取 localStorage admin token 认证
  - 功能：表格列表、搜索、状态筛选、分页、查看详情弹窗、单条/批量删除、未读角标
- 部署：PHP文件 + 路由 + SQL建表 + 静态文件全部 SSH 上传
- 验证 4/4 通过：
  - POST /api/consultation/add → 200 {"code":0,"msg":"提交成功，我们将尽快与您联系！"}
  - GET /api/admin/consultation/list (无token) → 200 {"code":1,"msg":"请先登录"}（正确拦截）
  - /admin/consultations.html → 200 (16.9KB)
  - /wg/js/main.js → 200 含 fetch API
- 访问方式：先登录 http://127.0.0.1/admin/ → 再打开 http://127.0.0.1/admin/consultations.html 查看留言

## 3D数字孪生引擎开发部署 (16:31-16:55)
- 分析原 3d-yanshi 代码：4个问题（文件名拼错、CSS引用错、模型文件0字节且文件名错误）
- 完全重写为程序化3D场景，无需外部模型文件
- 升级 Three.js 从旧版 script 标签 → ES Module 0.160 + importmap
- 场景组成：8栋建筑（住宅/商业/公共3种类型，带窗户发光+CSS2D标签）、环形+十字道路、20棵随机树木、中央水景喷泉（旋转环）、24个监控点（浮动动画）、200个数据流粒子、边界围墙
- UI：右上信息面板（建筑数/监控点/设备数/FPS）、左下颜色图例、加载动画
- 操作：OrbitControls（拖拽旋转缩放）、键盘快捷键 R重置/F俯视/T平视
- 输出文件：index.html(2KB)、style.css(2.6KB)、main.js(17.9KB)
- 部署到 /www/wwwroot/www.hbdxm.com/public/3d-yanshi/
- 线上验证 3/3 全部200 OK，内容正确
- 访问：https://www.hbdxm.com/3d-yanshi/

## 3D数字孪生 v2 超大规模升级 (16:55-17:00)
- 建筑从8栋 → 24栋（4座内环地标+8栋住宅+6栋商业+5栋公共设施+4栋办公）
- 新增建筑屋面系统：锥形塔尖/坡屋顶/设备平台，商业楼带入口雨棚+支柱
- 道路系统重写：南北+东西主干道（车道标记+人行道边）、环形道路、支路网
- 4个功能分区色块底色 + CSS2D区域标签（智慧住宅区/商业中心区/公共服务区/创新办公区）
- 新增约20辆移动车辆（8种颜色），主干道自动行驶、到边界循环
- 树木增至60+棵（圆形/锥形两种），沿道路+分区种植
- 中央广场重做：水池+水面波动动画+雕塑柱+顶部脉动球体+3层旋转喷泉环
- 监控点36个（分4区随机分布），粒子系统400个彩色数据流
- 键盘快捷键扩充：1/2/3/4 快捷跳转到4个分区
- 阴影贴图升级到4096分辨率，灯光增加半球光

## 3D数字孪生 v3 公司信息面板 (17:14)
- 左侧添加公司信息面板：#company-panel，含大圣Logo SVG、公司全称（杭州喵喵至家网络有限公司）、项目名（大圣智慧物业·数字孪生引擎）、slogan
- 右侧底部联系方式面板：#contact-panel，含电话17771300068/19171045360、邮箱mmzj@miaomiaozj.com、地址（杭州临平区怡丰城12幢）、官网www.hbdxm.com
- 电话和邮箱为可点击链接（tel:/mailto:），鼠标悬停高亮变色
- CSS动画：左侧面板从左边滑入，右侧面板从右边滑入
- 已部署上线，验证通过

## 3D数字孪生 v4 成熟小区重写 (17:21-17:30)
- 搜索了Sketchfab/CSDN/GitCode等平台，确认无免费可用的中国成熟小区GLTF模型
- 完全重写main.js为逼真中国成熟小区场景（翡翠名苑），14栋高层住宅(26-33层)
- 建筑：米白暖色调外墙、4户/层带窗、楼层分隔线、屋顶设备间、入口雨棚
- 小区大门：门柱+横梁+"翡翠名苑"发光牌匾+保安亭+车辆道闸
- 围墙：四面围墙带墙柱和顶饰
- 中央花园：六柱圆顶凉亭+圆形小池塘(水面波动)+石头+草坪+步道
- 休闲设施：儿童滑梯+秋千(绳索+座椅)、篮球场(篮架+篮圈)、户外健身区(4台器械)
- 社区会所：四坡屋顶+发光窗户
- 地下车库：3个入口坡道+顶棚+P标识
- 路灯系统：环小区道路路灯带发光灯球
- 监控：18个红色球体探头+支架+浮动动画
- 车辆：12辆道路车+8辆地面停放
- 快捷键：R重置/F俯视/G大门视角/1住宅区视角/2花园视角
- 信息面板更新：14栋楼/1680户/18监控点/320+在线设备
- 图例更新：高层住宅/绿化园林/社区配套/智能监控
- 待用户运行deploy.bat完成部署（本地36.4KB，线上仍是旧版33.9KB）

## 3D数字孪生 v5 多小区场景切换 (17:42-17:49)
- 左侧添加小区选择面板，4个不同社区场景可切换
- 翡翠名苑：14栋26-33层高层，中央花园+凉亭+篮球场+儿童区+会所
- 云栖别墅：48栋独栋3层别墅（8×6排布），红瓦屋顶，中央泳池+喷泉
- 中粮壹号：6栋42-55层玻璃幕墙超高层，发光窗户+天线，中央几何喷泉广场，商业裙楼
- 山水居：12栋花园洋房3层台地，叠水瀑布+石桥+池塘+中式凉亭×2+竹林+石园
- 键盘3/4/5快速切换小区
- 官网首页(qh/index.html)曾加错位置，后补正到根 public/index.html（www.hbdxm.com 首页 Banner）
- public/index.html 第60行添加了「小区数字建模演示」按钮（橙色），链接 /3d-yanshi/
- 部署：SSH2 SFTP 上传 public/index.html (20.9KB) 成功
- 线上验证：www.hbdxm.com/ 含按钮确认

## 3D数字孪生 v6 怡丰城真实建模 (17:53-18:00)
- 用户提供怡丰城平面图（高德地图截图），要求生成真实布局的3D建模
- 分析平面图：9栋建筑（3-11号楼）围绕中央广场呈网格排列，西门+东门双入口
- 新增 communities.yifeng 配置 + buildYifeng() 函数（约200行）
- 建模内容：
  - 9栋商业住宅楼（8-12层，按真实位置排布）：北排11/4/3号、中排10/5/6号、南排8/9/7号
  - 中央广场（38×24，地砖装饰）+ 怡丰城发光名牌 + 景观喷泉（雕塑+橙色球体顶饰）
  - 西门（S101省道侧）+ 东门（香乐路侧）双入口门楼
  - 内部道路网格（2条南北+2条东西主干道）
  - 11个沿街商铺配套（橙色+白色招牌）
  - 4个停车区（P标识）
  - 外围道路标注：S101省道、翁梅街、香乐路
  - 30+棵树木点缀、24个监控点、23辆车辆
- 键盘快捷键6切换到怡丰城场景
- index.html添加第5个选项"怡丰城-城市综合体·临平"
- main.js从36.4KB→67.7KB（含全部5个场景）
- SSH部署成功，线上验证全部通过（buildYifeng/怡丰城/西门/东门/S101省道 均true）

## IoT设备数据3D叠加 + AI报修助手 (20:08-20:30)
- 用户要求实现两个功能：1) 3D场景叠加IoT设备数据 2) AI报修助手
- **IoT设备数据层**：
  - 新建 `app/api/controller/IotData.php`，6种设备类型（烟感/门禁/水表/电表/摄像头/环境传感器）
  - 每个小区预设设备坐标，API返回模拟实时数据（含status/value/alarm）
  - main.js新增 buildIotLayer/clearIotMarkers/startIotPolling/stopIotPolling
  - 彩色发光球体标记：绿=正常 黄=警告 红=告警 灰=离线
  - 设备脉冲浮动动画 + 标签显示设备名和数值
  - 5秒自动轮询刷新，切换小区时重建设备层
- **AI报修助手**：
  - 新建 `app/api/controller/AiRepair.php`：chat(智能对话)/submit(提交报修)/quickTypes(快速类型)
  - 词库匹配：水电/空调/门窗/墙面/燃气/电梯/安保/卫生/停车 9种类型
  - 紧急关键词识别（紧急/爆管/着火/漏气等）→ 自动标记紧急
  - 位置提取（厨房/卫生间/客厅等）
  - 自动派单：RAND()随机匹配在线维修人员
  - 前端组件：3d-yanshi/ai-chat.css + ai-chat.js（右下角浮动聊天按钮+面板）
  - 快速类型按钮 + 打字动画 + 确认提交流程
- 新增路由：api/iot/devices、api/ai/chat、api/ai/submit、api/ai/quickTypes
- 部署验证：IoT API返回54设备47在线，AI Chat识别水电+厨房，AI Submit生成工单DSR202606082026079545自动派单给孙师傅
- 线上预览：https://www.hbdxm.com/3d-yanshi/ 右下角🤖按钮

## AI报修助手加入业主端 (20:30-20:33)
- 创建 Vue 3 版 AI 聊天组件 `frontend/src/owner/components/AiChatWidget.vue`
- 集成到 App.vue，登录后所有页面显示浮动按钮（tab bar上方，bottom:80px）
- 功能与3D版本一致：对话识别报修类型/位置/紧急程度、确认提交、自动派单
- 前端构建成功，JS bundle 46.1KB含 AiChatWidget
- 部署路径：public/owner.html + public/assets/owner-BSBc_xly.js + owner-Db6-MrBm.css
- 线上访问：https://www.hbdxm.com/owner.html 右下角🤖浮动按钮

## IoT设备数据库化 (20:35-20:53)
- 新建4张表：ds_iot_protocol(13种)、ds_iot_device_type(21种)、ds_iot_device(390台)、ds_iot_device_data(390条)
- 协议覆盖：MQTT/CoAP/HTTP/Modbus/BACnet/Zigbee/LoRaWAN/BLE/NB-IoT/OPC UA/Wi-Fi/RS485/4G Cat.1
- 设备类型：烟感/温感/燃气/CO/门禁/水电燃气表/摄像头/温湿度/PM2.5/噪音/水浸/液位/水压/电梯/消防栓/路灯/地磁车位/垃圾桶/井盖
- IotData.php 重写从DB读取（含3D场景编码→DB编码映射：feicui→YG001, yunqi→CY001, zhongliang→0004, shanshui→001, yifeng→YG001）
- 注意：服务器TP6的Db::field()不接受数组参数，Db::select()返回数组非Collection，无toArray()方法
- SQL生成器：database/iot_tables.sql + iot_seed.sql + iot_full_seed.sql（157KB，390设备+数据）
- API验证通过：feicui=179台(online=153/alarm=6/warning=9)、yunqi=74台、shanshui=70台

## 管理端IoT设备+AI助手管理 (20:53-21:15)
- 用户要求在admin后台加上IoT设备管理和AI助手管理，与3D-yanshi数据联动
- **IoT设备管理**：
  - 后端：app/admin/controller/Iot.php — deviceList/add/edit/delete/stats + typeList/typeAll/typeAdd/typeEdit/typeDelete + protocolList/protocolAll/protocolAdd/protocolEdit/protocolDelete + dataList
  - 前端：admin/src/views/iot/IotDevice.vue — 4个Tab（设备列表/设备类型/协议管理/实时数据），含统计卡片、筛选、CRUD弹窗、坐标编辑
  - 设备列表直接读取 ds_iot_device 表（与3D场景的 IotData/getDevices 同一数据源）
- **AI助手管理**：
  - 后端：app/admin/controller/AiAssistant.php — config(GET/POST读写关键词配置)/chatHistory(对话记录)/stats(统计趋势)
  - 前端：admin/src/views/ai/AiAssistant.vue — 3个Tab（配置/对话记录/数据统计含ECharts图表）
  - AI配置存于 ds_config 表(key=ai_type_keywords/ai_urgent_keywords/ai_greeting/ai_welcome_tips)
- 新建 ds_ai_chat_log 表记录对话历史
- **路由冲突解决**：admin路由最初用 `ai/*` 前缀，与公开 `api/ai/*` 产生TP路由缓存冲突导致404；改为 `aiAssistant/*` 后正常
- **TP路由缓存**：`runtime/routes.cache` 需要删除才能刷新路由注册，单纯清 cache/ 目录不够
- BaseAdmin权限映射新增：iot:device→Iot、ai:assistant→AiAssistant，manager角色白名单已追加 Iot 和 AiAssistant
- 路由注册：route/api.php 新增 21 条 admin 路由
- 前端构建输出：IotDevice-Cu4RIXUF.js(25.91kB) + AiAssistant内嵌在主bundle
- 全部4个API端点外部验证通过返回JSON

## 侧边栏菜单+端到端测试数据 (21:16-21:33)
- 用户要求菜单在侧边栏显示并写入端到端测试数据验证功能
- **侧边栏菜单系统**：菜单从 ds_menu 表读、按角色(role_menu)过滤、Login::info 接口返回树形JSON、Sidebar.vue 用 el-menu 渲染、图标从 iconMap 查找
- **菜单插入**：ds_menu 新增 ID=211(IoT设备管理, iot:device) + ID=212(AI助手管理, ai:assistant)，sort=14/15
- **角色关联**：role_id=1(超管)和2(系统管理员)均已分配两个新菜单
- **Sidebar图标**：iconMap 新增 `iot: 'Monitor'` + `assistant: 'MagicStick'`
- **Bug修复**：
  - ds_iot_device 表缺少 delete_time 列 → ALTER TABLE 添加（本地+服务器）
  - AiAssistant.php 中 `Db::name('config')->insert` 用了不存在的 `title` 字段 → 改为 `name`+`remark`
  - AiAssistant.php 联表查询 `o.name` → 改为 `o.realname`（ds_owner表实际字段名）
- **测试数据**（同步到服务器DB）：
  - IoT设备类型 8 种（温度/湿度/PM2.5/电表/水表/烟感/门禁/摄像头）
  - IoT协议 6 种（Modbus RTU&TCP/MQTT/BACnet/HTTP REST/LoRaWAN）
  - IoT设备 5 台（关联阳光花园小区，含3D坐标 x/y/z）
  - IoT实时数据 192 条（每设备24小时×5台，含value/unit/is_online/device_status）
  - AI配置 4 条（type_keywords/urgent_keywords/greeting/welcome_tips）
  - AI对话记录 8 条（owner_id关联真实业主，含漏水/电梯/灯/停水/暖气/物业费/异味/装修问答）
- **部署**：PHP文件4个(Iot.php/AiAssistant.php/BaseAdmin.php/api.php) + 前端244个assets + index.html
- **验证**：服务器DB行数确认通过，API路由均返回JSON（需登录），因验证码限制未能完全自动化端到端登录测试
- **访问方式**：登录 https://www.hbdxm.com/admin/ → 侧边栏"IoT设备管理"和"AI助手管理"可直接点击进入

## Request::isPost() 修复 (21:35)
- 用户报告 AI 智能报修助手报错：`Call to undefined method app\Request::isPost()`
- 根因：`app/Request.php` 缺少 `isPost()` 方法，`AiAssistant.php` 第14行调用失败
- 修复：给 `app/Request.php` 添加 `isPost()`、`isGet()`、`isAjax()` 三个便捷方法
- 部署：SSH上传 Request.php + 清除 runtime/temp 缓存

## 开发手册更新 (21:40)
- 用户要求搜索并更新美化项目的开发手册
- README.md 完全重写：更新技术栈(Vue3+Three.js)、新增3D/IoT/AI/WG等功能说明、美化排版(Badge+表格+架构图)
- DEVELOPMENT.md 升级到 v3.0：新增五.5章(3D数字孪生+IoT+AI架构详解)、IoT+AI数据表、路由、部署路径、AI关键词配置说明
- 关键内容：5小区渲染函数行号、IoT设备层5s轮询机制、小区编码映射表、AI词库JSON、数据联动全景图

## 开发手册HTML网页 (21:45)
- 用户要求按官网风格把开发手册写成HTML网页
- 创建 `public/wg/dev.html` — 完整开发者手册HTML页面，含10大章节全部内容
- 设计风格完全匹配官网：深蓝主色(#1a3a5c) + 橙色点缀(#f7931e) + 相同Header/Footer/字体/卡片
- 特色功能：左侧粘性目录导航(滚动高亮) / 4列统计概览 / 架构ASCII图 / 代码暗色块 / 安全机制卡片网格 / 路由列表 / 全响应式
- 已部署到 https://www.hbdxm.com/wg/dev.html

## GitHub推送 (21:58)
- 推送前需先 ssh-agent 加载密钥（Windows ssh-agent 服务需手动启动）
- 成功推送4个提交到 github.com/cxdxfx12/csy master
- 提交 5ccae9d 含时间戳 (2026-06-08 21:54)
- ⚠️ 注意：本机 git push 需要先启动 ssh-agent 服务并 ssh-add 密钥，否则 Permission denied

## 业主端白板修复 (22:06)
- 问题：http://www.hbdxm.com/owner.html 白板
- 原因：owner.html 引用了 `<link rel="modulepreload">` 的两个 JS 文件（GlobalToast-BgVsukJE.js、pinia-fR2eCVai.js）服务器上缺失 → 404 → Vue 无法初始化
- 修复：用 ssh2 上传这两个文件到 /www/wwwroot/www.hbdxm.com/public/assets/
- 验证：playwright-cli 确认页面正常跳转到 #/login，登录表单完整显示

## AI助手确认按钮修复 (22:11)
- 问题：业主端AI智能报修助手识别报修后，提示"回复「确认」即可提交"，但界面没有确认按钮
- 原因：AiChatWidget.vue 的 send() 函数只检查用户手动输入"确认"文字（第66行），无可视化确认按钮
- 修复：在消息区域新增 `.confirm-bar` 确认栏：
  - `pendingRepair` 存在时显示绿色「✅ 确认提交报修单」按钮 + 「取消」按钮
  - 新增 doConfirm() / cancelConfirm() 方法
  - 快捷标签在待确认状态时隐藏（避免干扰）
- 构建路径：`e:\ds/frontend` 目录的 vite build（非 admin/）
- 上传文件：owner.html + owner-ChOl9cIW.js + owner-Ct5ai1HB.css 到服务器
