# 2026-06-12

## 收费项目添加问题 (续) + SSE 实时推送连接循环修复 (00:13-00:30)

### 问题
- 用户反馈添加收费项目还是不行，F12 控制台大量错误：
  - `[Admin SSE] 实时推送已连接` → `连接断开` 反复循环（每3秒）
  - `V9 Object` 错误重复出现
  - 所有页面添加/编辑的 submitForm API 调用后缺少 catch 导致未处理 reject

### 根因分析
1. **SSE 重连死循环**：`HeaderBar.vue` 的 `onerror` 每次 3 秒固定延迟重连，无退避、无最大次数限制。快速重连导致日志刷屏
2. **submitForm 错误处理缺失**：之前批量修复了 validate 模式，但 API 调用部分缺少 catch。`request.ts` 拦截器在 code !== 0 时 `Promise.reject()`，submitForm 只有 finally 没有 catch → 未处理的 promise rejection → `V9 Object` 错误
3. **SSE 路由缺失**：`route/api.php` 的 `api/admin` 组中缺少 `sse/stream` 路由。config/route.php 设置 `url_route_must=true`，虽然实际可能通过 Nginx 或其他方式工作，但添加路由更可靠
4. 后端 API 测试正常（`curl` 测试 `/api/admin/charge/itemAdd` 返回了正确响应 `{"code":1,"msg":"数据已存在..."}` — 说明后端没问题）

### 修复
1. **SSE 重连退避**（`HeaderBar.vue`）：
   - 添加 `sseRetryCount`、`SSE_MAX_RETRIES=10`、`SSE_BASE_DELAY=2000`、`SSE_MAX_DELAY=60000`
   - `onerror` 改为指数退避：delay = min(2000 × 2^(重试次数-1), 60000)
   - connect 成功时重置 retryCount=0
   - 超过最大重试次数停止连接
   - 移除了冗余的 `error` 事件监听（与 onerror 重复）
   - `disconnectSSE()` 设置 retryCount 阻止重连

2. **ChargeItem submitForm** 添加 catch 块（错误已在拦截器中提示，此处仅防止未处理 reject）

3. **路由补充**：在 `route/api.php` 的 `api/admin` 组中添加 `Route::get('sse/stream', 'admin/AdminSse/stream')`

4. 构建部署 276 个文件全部同步到服务器

## 收费项目添加的真正根因：duplicate code 唯一索引冲突 (00:30-00:38)

### 问题
SSE/前端的修复后用户仍无法添加收费项目。端到端 API 测试发现返回 `{"code":1,"msg":"数据已存在，请勿重复添加"}`（来自 public/index.php 全局异常捕获的 Duplicate entry 翻译）。

### 根因
`ds_charge_item` 表有 `UNIQUE KEY uk_code (code)`，`code` 字段默认值为 `''`（空字符串）。
`ChargeItem::add()` 方法中从不生成 `code` 值，所有新插入的行 `code` 都是 `''`，MySQL 唯一索引冲突 → Duplicate entry 错误。

### 修复
1. **ChargeItem.php add()**：移除 `id` 字段（`unset($data['id'])`），自动生成唯一 code：`CI_` + 时间戳 + 随机后缀
2. **ChargeItem.php edit()**：移除 `id` 字段，保留原 code 值（如被清空则恢复）
3. **数据库**：执行 SQL 修复所有已存在的空 code 行：`UPDATE ds_charge_item SET code = CONCAT('CI_', ...) WHERE code = '' OR code IS NULL`

### 验证
三次连续 API 测试（带 code / 不带 code × 2）全部返回 `{"code":0,"msg":"添加成功"}`

✅ **用户确认修复生效，添加收费项目功能正常** (00:43)

## 同类隐患排查与修复：单字段 UNIQUE KEY + DEFAULT '' 的 add() 未自动生成 (00:43-00:48)

### 问题
用户担心其他菜单是否有同样问题。系统性排查了数据库中所有 18 个 UNIQUE KEY。

### 排查结果

**安全（add() 已自动生成或用户必填）：**
- `Bill` (`bill_no`) → `build_order_no('DSB')`
- `Payment` (`payment_no`) → `PAY` + timestamp + rand
- `RepairOrder` (`order_no`) → `build_order_no('DSR')`
- `AdminUser` (`username`) → 用户必填登录
- `Owner` (`phone`) → 用户必填
- 组合键表 (`role_menu`, `owner_room`, `parking_space`) → 复合键，不适用

**无 add() 方法（系统内部管理）：**
- `Finance`、`Complaint`、`Config`、`CommunityPaymentConfig`、`CommunityWechatConfig`

**⚠️ 找到 4 个同类隐患并已修复：**

| 表 | 唯一键 | 字段 | 前缀 | add() 修复 | edit() 修复 |
|---|---|---|---|---|---|
| `ds_role` | uk_code | code | RL_ | ✅ 空则自动生成 | ✅ 保留原值 |
| `ds_community` | uk_code | code | CM_ | ✅ 空则自动生成 | ✅ 保留原值 |
| `ds_access_card` | uk_card_no | card_no | AC_ | ✅ 空则自动生成 | ✅ 保留原值 |
| `ds_equipment` | uk_code | code | EQ_ | ✅ 空则自动生成 | ✅ 保留原值 |

### 部署
- 4 个 PHP 文件已上传到服务器
- 数据库中已存在的空 code/card_no 已通过 SQL 批量修复
- 全部操作完成，已验证部署成功

## GitHub 清理垃圾文件 (00:50)
- 提交 `0101a8e`：删除 4 个浏览器测试残留的垃圾文件，`.playwright-cli/` 加入 `.gitignore`

## 经理端权限验证 + staff 退出修复 (01:07-01:33)

### 经理端公告/投票/活动权限
- 微信登录用户 role_id=3，BaseManager 仅限制 role_id ∈ {1,2,3} → 可访问
- ManagerNotice/ManagerVote/ManagerActivity 全部可用，前端 8 个 Tab 全部显示
- API 实测通过（admin token 测试三个 list 接口全部 code:0）
- community_ids 为空时：能发但数据 community_id=0 成为孤儿，前端提示"暂无管理的小区"

### staff 退出修复
- **修复前**：`auth.removeToken()` + `router.replace('/login')`，逻辑本身正确但不够健壮
- **修复后**（`HomeView.vue`）：
  - 直接 `localStorage.removeItem('staff_token', 'staff_community_id', 'staff_badge_seen')`
  - try/catch 包裹，失败时 `localStorage.clear()` + `window.location.href='/staff.html'` 兜底
- 构建部署：`staff-DvzlWSDT.js` + `staff-pN1xxlph.css`，旧文件已清理

## staff 退出手机端兼容修复 (01:35-01:42)
- **根因**：`router.replace('/login')` 在移动端微信内置浏览器 WebView 中 hash 路由跳转不稳定，PC 正常但手机"静默失败"
- **修复**：改用 `window.location.replace('/staff.html')` 做全页面刷新跳转，与浏览器环境无关
- 构建部署：`staff-gfwAOReW.js` + `staff-B0av0FvW.css`，旧文件全部清理

## staff/owner/manager 退出跳转全局修复 (15:31)

### 问题
手机上点退出后跳到待接单页面，`window.location.replace()` 在某些移动端 WebView 中不触发完整页面重载。

### 修复
三个独立前端的 logout 全部改为 `window.location.href = origin + '/xxx.html?_t=' + Date.now()`：
- `staff/views/HomeView.vue` — `window.location.href = origin + '/staff.html?_t=' + Date.now()`
- `owner/views/HomeView.vue` — 同上，owner.html
- `manager/views/DashboardView.vue` — 同上，manager.html

使用 `window.location.href` + 绝对 URL + 时间戳防缓存，确保 WebView 完整重载到登录页。

### 部署
- 构建生成：staff-qmb8ZaKa.js, owner-DTxPEam3.js, manager-ByG-RGaT.js + 对应 CSS
- 通过 ssh2 SFTP 上传 28 个文件（3 HTML + 25 JS/CSS）到 `/www/wwwroot/www.hbdxm.com/public/`
- 全部成功，0 失败
- 服务器 SSH 密码确认为 cxdxfx12

## 微信 WebView 退出跳转修复 (15:41-15:46)

### 问题
手机浏览器退出正常，但**微信内置浏览器**打开后点退出仍有问题。根因：微信 WebView 会拦截同域 `location` 跳转，把完整页面重载变成 SPA 内部 hash 变化，导致 token 清了但页面没真正刷新。

### 修复
三个 logout 改为 `top.location.replace()` + `setTimeout(50ms)`：
```js
const url = origin + '/xxx.html?logout=' + Date.now()
setTimeout(() => {
  try { top.location.replace(url) } catch (_) { window.location.replace(url) }
}, 50)
```
- `top.location` 绕过微信 iframe 封装
- `setTimeout(50ms)` 确保 localStorage 写入完成
- `try/catch` 降级到普通 replace

### 部署
- 构建：staff-D9aGgBqz.js, owner-Du_JuSnc.js, manager-BaK1_OWo.js
- 12 个文件全部上传成功
- 端对端测试：17771300000 / cxdxfx12 账号登录-退出流程正常

### 测试工具
playwright-cli 可用路径：`C:\Users\ccc\AppData\Roaming\npm\playwright-cli.cmd`

### 最终确认 (15:51)
用户微信端真机测试通过，`top.location.replace()` 方案解决了微信 WebView 退出跳转问题。

## 系统管理员 vs 维修人员 — 员工登录冲突分析修复 (15:54-16:02)

### 架构分析
- `ds_admin_user`（系统管理-用户管理）和 `ds_repair_worker`（报修管理-维修人员）是独立两张表
- 登录用两级查表兜底：先 admin_user(username)，再 repair_worker(phone/name/job_no)
- JWT `source` 字段区分 admin/worker，防止跨表 ID 碰撞
- admin_user.phone 和 repair_worker.phone 相同时会自动关联（staffInfo 指向 worker）

### 修复 4 个问题

**P0: 维修工改密码 Bug** — `StaffLogin::password()` 硬编码 admin_user 表
- 改为根据 `staffInfo.is_worker` 分表操作，维修工走 repair_worker 表

**P1: 手机号重复风险** — `AdminUser.php` add/edit 中添加校验
- add(): 若 phone 已被 repair_worker 使用，直接拦截并提示工人姓名
- edit(): 仅在 phone 变更时校验

**P2: 小区隔离不一致** — admin_user 手机号匹配到维修工时，community_id 被覆盖为工人的单小区
- 改为优先使用 admin 的 community_ids，保留完整的 community_ids 字段
- 向后兼容路径同步修复

### 部署
- 3 个 PHP 文件上传到 `/www/wwwroot/www.hbdxm.com/app/`
- StaffLogin.php, BaseStaff.php, AdminUser.php — 全部成功
